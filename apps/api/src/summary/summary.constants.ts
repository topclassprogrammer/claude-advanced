export const CLAUDE_SUMMARY_MODEL = 'claude-opus-5';

export const SUMMARY_SYSTEM_PROMPT = `Ты помогаешь готовить выжимку встречи по её текстовой транскрипции.
Верни краткое summary встречи, список action items (конкретных задач) и список принятых решений.
Для каждого action item указывай ответственного (assignee), только если он явно назван в транскрипте — иначе оставляй null.
Если решений или задач не было озвучено, верни пустой список для соответствующего поля.`;

/**
 * Явное ограничение длины транскрипта перед отправкой в Claude API — вместо
 * непредсказуемой ошибки провайдера при превышении контекстного окна модели
 * длинными встречами (транскрипт обрезается детерминированно, с пометкой).
 * ~4 символа на токен для русского/английского текста; оставляет запас под
 * системный промпт и структурированный вывод в пределах контекстного окна.
 */
export const MAX_TRANSCRIPT_CHARS = 60_000;
export const TRANSCRIPT_TRUNCATION_NOTICE =
  '\n\n[транскрипт обрезан из-за превышения допустимой длины]';

/**
 * Ограничивает время ожидания ответа Claude API — без этого зависший запрос
 * держал бы выжимку в PROCESSING неопределённо долго вместо стабильного FAILED.
 */
export const CLAUDE_REQUEST_TIMEOUT_MS = 120_000;
